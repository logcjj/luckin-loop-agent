# 原始输入

```text
# AGENTS.md instructions

<INSTRUCTIONS>
#Goal Mode 工作流
当处于 goal mode，或用户 prompt 包含 /goal 时，必须进入本流程。

在当前涉及到的 project 下创建新的目录：

goal/goal-[num]/
  input.md
  plan.md
  tasks.md
编号递增，不得覆盖已有目录。

如过目前没有 project，你需要新建。

input.md：完整保存用户原始输入，逐字保留，不得改写。
plan.md：分析需求、上下文、风险、执行方案、验证方式、回滚方案。
tasks.md：把 plan 拆成小任务，每个 task 必须可独立验证。每三个task需要一次大型全面检查-debug循环，确保没有bug和问题。
在完成以上文件前，不得修改代码。

每次只执行一个 task。每次你想要结束task的时候，你必须思考：“你对当前实现100% 有信心吗？”如果没有，请找出所有可能的漏洞和提高的方案，提出合适的修复方案，然后不断重复这个循环，直到你对新实现在事实上达到100% 自信为止。
然后你需要提交代码（若有）
在tasks.md中把任务标记为完成并且写上你干的事情（你在tasks.md中需要留空用来干这些事）
然后你需要向用户简单汇报，并且停止输出，然后你会自动开始下一轮task

每次上下文压缩后，你必须全量读取这三个文件，以防止上下文模糊。

全部task完成后，你需要进行最后的最大的review，全面从c端，代码，安全性等角度分析项目，并且进行修缮和测试，直到完美为止。然后把goal标记为完成

goal完成后，你标记这轮goal所创建的goal文件夹为完成状态，然后归档整理。
如果goal文件过多，或者单goal文件内的字数过长，请对goal中间文件进行整理和压缩
</INSTRUCTIONS><environment_context>
  <cwd>/Users/logcjj/Documents/GitHub/luckin-agent-submission</cwd>
  <shell>zsh</shell>
  <current_date>2026-07-20</current_date>
  <timezone>Asia/Shanghai</timezone>
  <filesystem><workspace_roots><root>/Users/logcjj/Documents/GitHub/luckin-agent-submission</root><root>/Users/logcjj/.codex/visualizations/2026/07/20/019f7dfc-1fb3-7003-8c45-aec2bf3387e4</root></workspace_roots><permission_profile type="disabled"><file_system type="unrestricted" /></permission_profile></filesystem>
</environment_context>
```

```text
现在我们继续完善整个内容显示，美观程度以及深度。包括手机端什么的，参考参考[https://github.com/bcefghj?tab=repositories](https://github.com/bcefghj?tab=repositories) 他的项目，不仅仅是小满那个，还有很多，参考人家的深度，内容，agent配置，整体的架构设计，去模仿一下，优化一下我们的项目，包括美术，技术实现，风格（结合瑞星咖啡的官方啥的），包括要不要接什么真实的咖啡信息，以及会员信息，你现在这个有点太空洞了没有深度，咱们去模仿一下他的多个项目，
```

```text
注意风格，你这个太丑了，还有内容啥的，你自己看看，跟别人的差了十万八千里
```

```text
一个一个做，这个GUI-Agent只是执行功能那部份，agent对话部分还得重新做（包括深度，内容，配色gui），还有很多差的
```

```text
注意我跟你说的，中间的内容等部分，整个还是参考小满那个，只不过咱们的包括（深度，内容，配色gui，包括整体的agent设计还不行，你严格去抄他那个），然后我说一下那个自动执行，以及效果模拟，是另一个模块需要做的，如果点执行，会去显示
```

```text
现在我们继续完善整个内容显示，美观程度以及深度。包括手机端什么的，参考参考[https://github.com/bcefghj?tab=repositories](https://github.com/bcefghj?tab=repositories) 他的项目，不仅仅是小满那个，还有很多，参考人家的深度，内容，agent配置，整体的架构设计，去模仿一下，优化一下我们的项目，包括美术，技术实现，风格（结合瑞星咖啡的官方啥的），包括要不要接什么真实的咖啡信息，以及会员信息，你现在这个有点太空洞了没有深度，咱们去模仿一下他的多个项目，注意配色动画，真实性，等等等等，咱们目前的模块深度啥的都不行，你去看别人，包括手机端网页端，地理位置，商品信息（带真实图片），啥的你都要真实设计去弄，如果有需要，去爬去商品信息，整个完善一下，bcefghj有很多项目，许多都可以参考。不是直接换成蓝白就完了，你的审美非常差，你要参考别人的，然后目前的内容都是几个大框框一点没有深度，你严格参考别人的，现在效果上还是差了十万八千里。这样吧，你严格分析一下我发给你的那个人的几个高级项目，详细看。然后自动点单那个模块，我觉得你可以参考https://github.com/UAVLLMs/GUI-Agent 这个人的这个东西，咱们不是还有一个模块是自动执行吗？我想搞成你手机端也可以对这个进行模拟，有一个小窗显示执行进度，然后大窗自动操作。注意本身agent对话也要模仿小满弄成网页和手机两个
```

```text
你改的差死了，手机端刚开始也是和小满的一样啊（聊天），只不过如果点击执行，可以去模拟点单的过程，手机上可能有一个弹窗显示链路懂我意思吧
```

```text
现在差的非常多，你懂吗？
1.我让你参考小满以及bce的其他模块设计，你这个左边全是一些ai的东西，需要什么模块不合理
2.中间应该是一个大的窗口，引导或者聊天，不是写死的（参考小满）
3.右边应该是一些画像，执行什么的（原来写的挺好的）
4.手机端和网页端的切换也不行，应该在右上角
5.请完全参考小满，不要自己思考（所有的风格模块都参考人家，内容是自己的，切换到手机之后可以进行模拟，请严格自己参考去弄）
```

```text
1.你这个手机人家的不是一样的，你仔细看看人家的，就是显示的一个手机
2.感觉需要用户端和管理员端，你这个明显偏管理员端的显示，应该可以切换
用户端你不应该把场景订好了，对吧，不能把输入的内容写死了
然后如果是管理员，显示的东西应该变一下
3.整个风格美术，显示的东西应该通俗易懂（不是说词简单，你看看别人叫什么名字），人性化一点，不要太ai了
```

```text
现在差的非常多，你懂吗？
1.我让你参考小满以及bce的其他模块设计，你这个左边全是一些ai的东西，需要什么模块不合理
2.中间应该是一个大的窗口，引导或者聊天，不是写死的（参考小满）很重要，现在都是写死的
3.右边应该是一些画像，执行什么的（原来写的挺好的）
4.手机端和网页端的切换也不行，应该在右上角
5.请完全参考小满，不要自己思考（所有的风格模块都参考人家，内容是自己的，切换到手机之后可以进行模拟，请严格自己参考去弄）
6.手机点单请接入真实的，现在太假了
```

```text
不要参考场景，要通过和用户聊天去引导，参考小满
```

```text
不要显示用户的输入信息，你要引导用户，可以适当接入llm：
apikey是[REDACTED_QWEN_API_KEY]，兼容 OpenAl 接口协议工具 [https://coding.dashscope.aliyuncs.com/v1](https://coding.dashscope.aliyuncs.com/v1) 兼容 Anthropic 接口协议工具 [https://coding.dashscope.aliyuncs.com/apps/anthropic](https://coding.dashscope.aliyuncs.com/apps/anthropic) 这个是qwen的apikey，代码里需要接入
```

```text
这样吧，你把我们这套完全抛弃吧，只保留需求的东西，把在小满的基础上进行修改得了，可以吧
```

```text
用户输入的信息不用乱吗啊，可以显示
```

```text
这样吧，你把我们这套完全抛弃吧，只保留需求的东西，把在小满的基础上进行修改得了，可以吧，你先复刻一下小满，然后在人家的功能上直接去改
```

```text
是这样的，咱们的完全不要了，你直接把他那个下载了，然后改
```
